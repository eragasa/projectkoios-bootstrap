# Archive moved

Archive files have been moved out of `projectkoios-bootstrap` to:

`/Users/eugene/repos/projectkoios-spec/archive/`

This directory is retained only as a pointer so old `docs/archive/...` references have an obvious migration target.
