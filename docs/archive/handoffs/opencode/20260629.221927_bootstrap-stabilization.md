Origin: pi
Created: 2026-06-29 22:19
From: pi
To: Vulcan
Status: complete

# Task: bootstrap stabilization

## Context

We are in the single project `projectkoios-bootstrap`.
The user is manually switching between harnesses.

Observed state:
- Archon was unavailable for the original review run.
- `tmux` is not installed or not on PATH in the current environment, so the
  `./scripts/koios harnesses ...` commands cannot start or inspect the session here.
- I already applied two lightweight repo fixes:
  - `src/python/projectkoios/bootstrap/commands/harnesses.py`
    - fixed `_in_tmux()` to use `os.environ` instead of invalid `sys.environ`
  - `scripts/README.md`
    - fixed `docs/architecture.00.md` → `doc/architecture.00.md`

## Request

Please take the next implementation step for bootstrap stability, scoped only to
`projectkoios-bootstrap`.

Focus on the smallest useful follow-up:
1. verify the bootstrap/runtime path
2. fix any remaining repo-local issues that block starting or inspecting the koios session
3. keep docs and code consistent with the current `doc/architecture.00.md` path

## Constraints

- Do not assume tmux can be launched from inside a harness.
- Do not widen scope beyond this repo.
- Keep the work implementation-ready and validation-backed.

## Expected output

Return an implementation report with:
- files changed
- validations run
- any remaining blockers
- whether the repo is ready for the next harness startup step
