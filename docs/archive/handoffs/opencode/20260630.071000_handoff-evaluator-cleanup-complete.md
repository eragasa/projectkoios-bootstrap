Origin: Vulcan
Created: 2026-06-30 07:10
From: Vulcan (opencode)
To: Hermes
Status: complete

# Implementation report: handoff evaluator cleanup

## Changes

### Dead code removal
- Deleted `data/place.py`, `data/provenance.py` — unused DataObjects
- Deleted `actions/base.py` — unused abstract guard base
- Deleted `actions/` directory — moved to `handoffs/`

### Renames
- `ArtifactToken` → `HandoffArtifact`
- Test file renamed to match: `__ArtifactToken__construction__creates_token.py` → `__HandoffArtifact__construction__creates_token.py`

### API changes
- `ViolationAppender` class → `append_violations()` function (no `dry_run`)
- `Violation.token_path: str` → `Violation.path: Path`
- `ViolationCode(StrEnum)` replaces freeform string codes
- `Marking.all_tokens` property added; `tokens_for_harness()`, `find_contradictory()` removed
- `HandoffEvaluator.guards` type: `list[tuple[str, Callable]]` → `list[Callable]`
- Guards use set membership (`HERMES_IDS`, `VULCAN_IDS`, `CODEX_IDS`) and `marking.all_tokens`/`tokens_at()`
- `HandoffParser.__init__` no longer takes `handoffs_root`
- `HandoffParser.parse_named_directories()` removed
- `HandoffArtifact.authority_level` field removed

### Layout
- `actions/guards.py` → `handoffs/guards.py`
- `actions/appender.py` → `handoffs/appender.py`
- All test files moved from `tests/harness/actions/` → `tests/harness/handoffs/`
- Source `actions/` directory deleted

## Validation
- pytest: 51/51 passed
- ruff: all checks passed
- mypy: no issues in 22 source files
