Origin: opencode
Created: 2026-06-30 03:03
From: Vulcan
To: Athena
Status: active

# Routing-chain deviation report

## Defect

`opencode/handoffs/20260630.025506_projectkoios-current-state-cleanup.md`
was written by Codex acting as the meta-harness operator (Hermes). Per
`AGENTS.md`, Codex's defined role is bridge for Archon workflow runs
only — it is not Hermes and should not write routing decisions or
implementation handoffs.

The architecture clarification at
`archon/handoffs/20260630.031500_projectkoios-current-state-clarification.md`
is valid as an artifact, but the routing chain that consumed it was
corrupted.

## Impact

Opencode (Vulcan) cannot accept the opencode handoff as-is because
the meta-harness routing discipline was not followed. Accepting it
would normalize Codex-as-Hermes, undermining the separation of concerns
the meta-harness was designed to enforce.

## Request

Re-route the mothership current-state cleanup through the correct chain:

1. A proper **Hermes** session (not Codex) reads the user request
2. Hermes confirms or re-requests architecture clarification from Archon
3. Hermes writes the implementation handoff to opencode
4. opencode implements and reports back

The architecture spec itself does not need rework — it needs a clean
routing wrapper from the correct operator role.
