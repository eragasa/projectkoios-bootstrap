# AAR 20260702.053445: Session start incubator and spike check

## Scope
Startup instruction refinement.

## What happened
Updated the session-start checklist in `AGENTS.md` to include `docs/incubator/` and `docs/spikes/` alongside draft ADR review.

## Process issues
The user referenced `./doc/incubate` and `./doc/spike`, but the repo paths are `docs/incubator/` and `docs/spikes/`.

## Proposed follow-up improvements
Keep path names aligned with repository layout when adding startup checks.

## Candidate ADR or implementation topics
None.

## Current status
Completed; startup instructions updated and graph refreshed.
