# AAR 20260701.130032: ADR template extracted

## Scope

Canonical ADR format extraction into a reusable template.

## What happened

Inspected the repository's ADR-related source and policy surfaces, then rewrote `docs/templates/ADR.proposal.template.md` to match the canonical ADR shape and normalized section names. Also updated `archon/workflows/create-adr.yaml` to generate the same format.

## Process issues

No durable process issue observed.

## Proposed follow-up improvements

- Consider linking the ADR template from the docs index or template index if one is added.

## Candidate ADR or implementation topics

- Template/index for docs/templates/

## Current status

The ADR template now matches the canonical proposal format used in the repository.
