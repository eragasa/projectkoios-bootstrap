# AAR 20260701.124612: Agent charter canonical routing

## Scope

Promoted `docs/agent-charter.md` to the canonical routing doc and reduced overlapping role language in the local harness docs.

## What happened

Updated the charter to accepted status, then added pointers from `docs/meta-harness.md`, `pi/AGENTS.md`, and `opencode/AGENTS.md` so the role split is referenced once instead of repeated in full.

## Process issues

- The repo had overlapping role descriptions in multiple places.
- Canonical routing needed an explicit home before pruning duplicates.

## Proposed follow-up improvements

- Keep role changes centralized in `docs/agent-charter.md`.
- Add a short review checklist for any future role/doc updates.

## Candidate ADR or implementation topics

- Formalize canonical-doc precedence for bootstrap routing docs.

## Current status

Complete.
