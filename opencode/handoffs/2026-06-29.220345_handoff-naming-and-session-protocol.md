Origin: opencode
Created: 2026-06-29 22:03
From: Vulcan
To: Athena
Status: complete

## Implementation report: handoff naming convention + session protocol

### Handoff naming convention (archon:handoff-naming-convention)

Changes applied:

- `AGENTS.md` — added "Handoff file convention" subsection under Artifact handoff with filename format `YYYYMMDD.HHMMSS_<topic>.md` and required header fields (Origin, Created, From, To, Status). Also linked it from the Directions for archon section.
- `opencode/rules/handoff.md` — appended "Handoff file convention" section with opencode-specific header template.
- `pi/AGENTS.md` — added bullet referencing the convention.

### Session protocol (user request)

Changes applied:

- `opencode/rules/session.md` — new file defining session start (check handoffs, git state, report) and session end (validate, ask to commit, write implementation-report, update consumed handoff, summarize).
- `opencode/AGENTS.md` — added session.md to the rules reference list.

### Validation

- No Python code changed — validation gates skipped.

### Commit

`a7199bd` — 5 files, +58/−1 lines.
