Origin: pi
Created: 2026-06-30 07:00
From: Hermes
To: Vulcan
Status: active
Scope: projectkoios-bootstrap
Repository: /Users/eugene/repos/projectkoios-bootstrap

# Implementation brief: clean up handoff evaluator design/API/naming

## Objective

Refine the first-slice handoff evaluator committed at f58c1ad.
Remove dead code, co-locate modules, tighten the API, and align naming.

## Issues to fix

### 1. Remove dead code
Delete or strip these unused items:

- `data/place.py` (entire file)
- `data/provenance.py` (entire file)
- `actions/base.py` (entire file)
- `parser.parse_named_directories()` (unused)
- `parser.handoffs_root` param (stored but never read)
- `Marking.tokens_for_harness()`, `find_contradictory()` (unused)
- `guards.run_all_guards()` (unused)

### 2. Co-locate `actions/` into `handoffs/`
Move `guards.py` and `appender.py` from `harness/actions/` into `harness/handoffs/`.
Delete the now-empty `actions/` directory. Update all imports.

### 3. Rename ArtifactToken -> HandoffArtifact
Update class name in `data/artifact.py` and all references.

### 4. Fix Violation API
- `token_path: str` -> `path: Path`
- `code: str` -> `StrEnum` for the 4 known codes

### 5. Clean up guards
- Use set membership instead of chained `or`
- Use `marking.tokens_at()` instead of accessing `tokens_by_place` directly

### 6. Clean up HandoffEvaluator
- Remove unnecessary guards tuple wrappers
- Remove dead imports

### 7. Fix ViolationAppender / CLI split
- Remove dry_run from appender, let CLI handle it

## Files to change

- Delete: `data/place.py`, `data/provenance.py`, `actions/base.py`, `actions/`
- Move: `actions/guards.py->handoffs/`, `actions/appender.py->handoffs/`
- Rename class + update: `data/artifact.py`, `marking.py`, `parser.py`, `evaluator.py`, `guards.py`, `appender.py`, `commands/handoff.py`
- Update tests: move from `tests/harness/actions/` to `tests/harness/handoffs/`

## Validation

```
pytest
ruff check .
mypy src/python
```
