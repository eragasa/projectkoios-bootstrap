Origin: archon
Created: 2026-06-30 03:34
From: Athena
Acting-As: archon
To: Vulcan
Status: active
Scope: projectkoios-bootstrap
Repository: /Users/eugene/repos/projectkoios-bootstrap
Delegated-Operator: Codex

# Handoff: Harness role clarification docs update

## Objective

Implement the docs-only harness role clarification in
`/Users/eugene/repos/projectkoios-bootstrap` so the repository clearly
distinguishes Codex, Archon/Athena, pi, opencode/Vulcan, and Goose/Koios.

This handoff is for Vulcan implementation. Codex only packaged the completed
Athena plan for opencode consumption.

## Controlling spec

Read first:

`/Users/eugene/repos/projectkoios-bootstrap/archon/handoffs/20260630.033056_harness-role-clarification.md`

That Athena artifact is the controlling architecture/specification artifact for
this implementation slice.

## Target repository

`/Users/eugene/repos/projectkoios-bootstrap`

Observed packaging-time state:

```text
## master...origin/master
```

Treat any later local changes as pre-existing. Do not revert unrelated user or
agent changes.

## Scope

Docs-only clarification pass.

Primary files to inspect and likely edit:

- `AGENTS.md`
- `pi/AGENTS.md`
- `doc/meta-harness.md`

Possible supporting file only if direct inspection finds contradictory wording:

- `README.md`

Do not modify workflow YAML, Python code, maps, or sibling repositories for this
slice unless a contradiction blocks the docs update.

## Required changes

### Root `AGENTS.md`

- State explicitly that Codex is not `pi`.
- Define Codex as the delegated access/operator layer for invoking Archon and
  relaying Archon artifacts when direct pi ownership is unavailable.
- State that `pi` is the Project Koios meta-harness/router.
- State that `pi` routes user work to individual Project Koios repositories.
- State that each repository may run its own repo-local meta-harness instance.
- Distinguish bootstrap-shared guidance in `projectkoios-bootstrap` from
  repo-local handoffs, run state, and operational artifacts in target repos.
- Replace or rewrite wording equivalent to "Codex may perform pi actions" if it
  implies Codex identity collapse rather than delegated mediation.
- Extend the handoff provenance guidance to define:
  - `Origin`
  - `From`
  - `Acting-As`
  - `Scope` or `Repository`
  - `Delegated-Operator`

### `pi/AGENTS.md`

- Describe `pi` as the router/operator harness role.
- State that `pi` routes work into repo-local harness flows.
- Preserve repository-scoped execution and handoff boundaries.
- Avoid any wording that implies Codex is itself `pi`.
- Add guidance that handoffs preserve delegated-operator provenance when Codex
  or another mediation layer is involved.

### `doc/meta-harness.md`

- Align terminology with the clarified model if the file contains contradictory
  global-vs-local or Codex-vs-pi language.
- Keep changes narrow. Do not rewrite the whole framework unless needed to
  remove a direct contradiction.

### Optional `README.md`

- Only update if it contains stale operator-path wording that contradicts the
  clarified model.

## Non-goals

- Do not implement a new handoff validation command.
- Do not change Archon workflow provider configuration.
- Do not redesign every downstream repository's harness layout.
- Do not make product/domain architecture decisions.
- Do not modify sibling repositories.
- Do not claim that Codex replaces `pi`.
- Do not flatten repo-local artifacts into a single global bootstrap run-state
  model.

## Acceptance criteria

Implementation is complete when:

1. `AGENTS.md` explicitly states that Codex is a delegated access/operator
   layer for Archon invocation and artifact relay, not `pi`.
2. `AGENTS.md` explicitly states that `pi` routes user work to individual
   Project Koios repositories.
3. `AGENTS.md` explicitly states that each repository may run its own local
   meta-harness instance.
4. `AGENTS.md` explicitly distinguishes bootstrap-shared guidance in
   `projectkoios-bootstrap` from repo-local meta-harness artifacts/state.
5. `pi/AGENTS.md` describes `pi` as the router/operator harness role without
   implying that Codex is `pi`.
6. `pi/AGENTS.md` preserves repository-scoped execution and handoff boundaries
   for local meta-harness instances.
7. Updated docs remove or rewrite any wording equivalent to "Codex may perform
   pi actions" if that wording implies identity collapse instead of delegated
   mediation.
8. Updated docs define or exemplify provenance fields that distinguish
   `Origin`, `From`, `Acting-As`, `Scope`/`Repository`, and
   `Delegated-Operator`.
9. Updated docs remain internally consistent about Archon/Athena as the
   planning/spec harness, opencode/Vulcan as the implementation harness,
   Goose/Koios as the knowledge harness, and `pi` as the meta-harness/router.
10. No updated doc claims that repo-local artifacts or local run state for
    downstream repositories are owned by `projectkoios-bootstrap`.

## Validation

Run the smallest relevant validation in
`/Users/eugene/repos/projectkoios-bootstrap`.

Expected checks:

```bash
python -m pytest
python -m ruff check .
python -m mypy src/python
```

If a tool is unavailable, report it explicitly and run the checks that are
available.

Also run targeted text checks:

```bash
rg -n "Codex may perform|Codex.*as pi|Codex.*is pi|not `pi`|Delegated-Operator|Acting-As|Scope:|Repository:" AGENTS.md pi/AGENTS.md doc README.md
rg -n "repo-local|local meta-harness|individual Project Koios repositories|bootstrap-shared|repo-local meta-harness artifacts" AGENTS.md pi/AGENTS.md doc README.md
```

Use the search output to verify clarity, not to force every term into every
file.

## Expected output back to pi

Write an implementation report to `opencode/handoffs/` with:

- files changed
- validation commands and results
- deviations from the Athena plan, if any
- remaining ambiguity or follow-up architecture questions
- whether Archon review or Goose knowledge capture should happen next
