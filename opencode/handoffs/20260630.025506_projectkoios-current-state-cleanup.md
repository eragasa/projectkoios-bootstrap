Origin: pi
Created: 2026-06-30 02:55
From: Codex
To: Vulcan
Status: pending

# Handoff: Project Koios mothership current-state cleanup

## Objective

Implement the architecture-guided, docs-only current-state cleanup in
`/Users/eugene/repos/projectkoios` so the mothership repository accurately
describes its post-extraction role before the next MVP is selected.

## Controlling spec

Read first:

`/Users/eugene/repos/projectkoios-bootstrap/archon/handoffs/20260630.031500_projectkoios-current-state-clarification.md`

That Archon/Athena artifact is the controlling architecture clarification for
this implementation slice.

## Target repository

`/Users/eugene/repos/projectkoios`

Observed state before this handoff was written:

```text
## main...origin/main [ahead 1]
 M .gitignore
 M docs/architecture.md
?? .pi/
```

Treat those as pre-existing local changes. Do not revert them without explicit
user instruction. Work with them where relevant.

Recent target-repo commit observed:

```text
5eeee9a extract api, search, vault, indexing, runtime, ingestion to sibling repos
```

## Authoritative context

Use these sources in this order for this cleanup:

1. explicit user instruction
2. current filesystem and git state in `/Users/eugene/repos/projectkoios`
3. passing validation in the target repository
4. Archon clarification artifact listed above
5. bootstrap maps:
   - `/Users/eugene/repos/projectkoios-bootstrap/maps/repositories.md`
   - `/Users/eugene/repos/projectkoios-bootstrap/maps/packages.md`
6. accepted ADRs in `/Users/eugene/repos/projectkoios/docs/`
7. `projectkoios-notes` as knowledge context only

Current bootstrap maps say:

| Responsibility | Repository |
|---|---|
| Shared abstractions: chunking, repositories | `projectkoios` |
| Search & indexing | `projectkoios-search` |
| HTTP API & runtime wiring | `projectkoios-api` |
| Obsidian vault management | `projectkoios-obsidian` |
| References & citations | `projectkoios-references` |
| Workflow engine & provenance | `projectkoios-workflow` |
| Agent harness | `projectkoios-agent` |
| Meta-harness config & bootstrap CLI | `projectkoios-bootstrap` |

## Scope

Docs-only current-state cleanup.

Expected files to touch in `/Users/eugene/repos/projectkoios`:

- `README.md`
- `ROADMAP.md`
- `docs/architecture.md`
- one new ADR or current-state note under `docs/`

Possible supporting updates only if direct inspection finds a real mismatch:

- `/Users/eugene/repos/projectkoios-bootstrap/maps/packages.md`
- `/Users/eugene/repos/projectkoios-bootstrap/maps/repositories.md`

If a bootstrap map update is needed, stop and report the mismatch before editing
this repo. The expected path is that no bootstrap map edit is needed.

## Required changes

### README

- Replace stale component names such as `projectkoios-vault`,
  `projectkoios-ingest`, `projectkoios-schemas`, and `projectkoios-cli`.
- Describe the current sibling-repo layout using the bootstrap repository map.
- State that `projectkoios` is now:
  - product-level architecture and ADR home
  - transitional shared-code home for `projectkoios.chunking` and
    `projectkoios.repositories`
  - current-state reference point for the namespace package layout
- State that `projectkoios` is not the canonical implementation home for
  search/indexing, API/runtime, Obsidian/vault management, ingestion, agent
  runtime, or workflow engine.

### ROADMAP

- Remove or replace the claim that `projectkoios-agent` is the first separate
  implementation repo.
- Reflect that extraction has already happened.
- Reframe near-term priorities around mothership current-state cleanup first,
  then selecting the next MVP from a clean baseline.

### docs/architecture.md

- Separate current state from long-term vision.
- Remove or rewrite old single-repo package-tree claims so they are not
  presented as current state.
- Reflect the reduced code footprint: `projectkoios.chunking` and
  `projectkoios.repositories` remain in the mothership for now.
- Avoid turning this into a broad architecture rewrite.

### New ADR/current-state note

- Add a small follow-up ADR or current-state note under `docs/`.
- Do not heavily rewrite
  `docs/adr.20260629.namespace-and-ingestion-extraction.md`; preserve it as a
  historical decision.
- The new note should record:
  - extraction has already completed for ingestion/search/API/Obsidian-related
    runtime areas
  - the mothership now retains architecture docs plus `chunking/` and
    `repositories/`
  - retained packages are transitional shared abstractions for now

## Non-goals

- Do not move code between repositories.
- Do not extract `projectkoios.chunking` or `projectkoios.repositories`.
- Do not implement or modify `projectkoios-agent`, `projectkoios-workflow`, or
  `projectkoios-obsidian`.
- Do not modify `/Users/eugene/repos/projectkoios-notes`.
- Do not commit `.pi/` or any other local runtime state.
- Do not rewrite accepted ADR history beyond adding a superseding/current-state
  note.
- Do not perform a broad roadmap rewrite beyond current-state correction.

## Acceptance criteria

Implementation is complete when:

1. `README.md` describes the current sibling-repo layout and current mothership
   role.
2. `ROADMAP.md` no longer claims `projectkoios-agent` is the first extracted
   implementation repo.
3. `docs/architecture.md` no longer presents the old single-repo package tree as
   current state.
4. A new ADR or current-state note records post-extraction state without
   corrupting historical ADR intent.
5. The resulting docs explicitly say `projectkoios.chunking` and
   `projectkoios.repositories` remain in the mothership for now.
6. No updated doc implies search/API/Obsidian/ingestion still belong in the
   mothership.
7. The cleanup output makes clear that mothership current-state cleanup precedes
   new planned MVP work in agent/workflow/obsidian.

## Validation

Run the smallest relevant validation in `/Users/eugene/repos/projectkoios`.

Expected baseline checks:

```bash
python -m pytest
python -m mypy -p projectkoios
python -m ruff check .
```

If the local environment lacks a tool, report that explicitly and run the checks
that are available. Since this is docs-only, also run targeted text searches for
stale ownership claims, for example:

```bash
rg -n "projectkoios-vault|projectkoios-ingest|projectkoios-schemas|projectkoios-cli|first separate implementation repo|first extracted" README.md ROADMAP.md docs
rg -n "projectkoios\\.(search|indexing|api|runtime|obsidian|ingestion)" README.md ROADMAP.md docs
```

The second search may return historical ADR context; do not erase history just
to make the search empty. Instead, verify current-state docs are clear and
historical docs are framed as historical.

## Expected output back to pi

Write an implementation report to `opencode/handoffs/` with:

- files changed
- validation commands and results
- any pre-existing target-repo changes preserved
- any deviation from the Archon clarification
- whether Archon review or Goose knowledge capture should happen next
