Origin: Codex
Created: 2026-06-30 02:47
From: Codex
To: Codex
Status: active

# Handoff: Project Koios current-state routing

## Objective

Reconcile current `projectkoios` mothership repository state,
`projectkoios-notes` knowledge context, and bootstrap meta-harness routing rules
to decide the next bounded work slice.

This is a Codex-to-Codex continuity package. It is not an Archon architecture
specification and not an implementation report.

## Evidence gathered

### `~/repos/projectkoios`

- Directory exists at `/Users/eugene/repos/projectkoios`.
- Existing Graphify database exists at
  `/Users/eugene/repos/projectkoios/graphify-out/graph.json`.
- Graphify query used the existing graph rather than rebuilding it:

```bash
graphify query "Summarize this repository's main components and identify concrete next work candidates based on code, docs, TODOs, tests, and architecture signals. Prefer actionable repo-local tasks over broad strategy." --budget 2500
```

- Graphify result emphasized:
  - `docs/architecture.chunker.md`
  - `docs/architecture.repositories.code.md`
  - `projectkoios.chunking`
  - `projectkoios.repositories`
- Current repo state observed:

```text
## main...origin/main [ahead 1]
 M .gitignore
 M docs/architecture.md
?? .pi/
```

- Recent commit observed:

```text
5eeee9a extract api, search, vault, indexing, runtime, ingestion to sibling repos
```

- Current tracked code is small and centered on:
  - `src/python/projectkoios/chunking/`
  - `src/python/projectkoios/repositories/`
  - corresponding tests under `tests/projectkoios/`

### `~/repos/projectkoios-notes`

- Directory exists at `/Users/eugene/repos/projectkoios-notes`.
- It is not a git repository.
- Existing Graphify database exists at
  `/Users/eugene/repos/projectkoios-notes/graphify-out/graph.json`.
- Graphify query used the existing graph:

```bash
graphify query "Summarize the Project Koios notes and identify what they imply about next work, especially for mothership role, bootstrap/meta-harness, obsidian, ingestion, workflow, and agent direction. Prefer concrete actionable items and conflicts with repository state." --budget 3000
```

- Graphify result emphasized:
  - `projectkoios-bootstrap.architecture.00.md`
  - `projectkoios-obsidian.workflow.00.md`
  - bootstrap lifecycle and state ownership
  - workflow ownership
  - validation gates
  - architectural risks
- Notes signal:
  - `projectkoios-bootstrap` is temporary scaffolding, not the domain engine.
  - Long-term Project Koios should own its own workflow, provenance, ingestion,
    validation, knowledge curation, and command interface.
  - `projectkoios-obsidian` and `projectkoios-workflow` are design targets.
  - Risks include bootstrap becoming the domain engine, hidden chat state,
    duplicate package creation, provider lock-in, and vault mutation without
    provenance.

### `projectkoios-bootstrap`

- Current repo is `/Users/eugene/repos/projectkoios-bootstrap`.
- Latest local commit:

```text
4c96b5b Add bootstrap flow stabilization use case
```

- This repo now documents specialist routing discipline:
  - use the operator directly for small, reversible, repo-local work;
  - use Archon for design ambiguity, policy conflict, cross-harness impact, or
    unclear acceptance criteria;
  - use opencode for nontrivial implementation or deeper validation;
  - use Goose for durable knowledge/provenance capture.

## Current conflicts and uncertainties

- `ROADMAP.md` in `projectkoios` still says the first separate implementation
  repo will be `projectkoios-agent`, but repo history shows multiple components
  have already been extracted.
- `docs/adr.20260629.namespace-and-ingestion-extraction.md` lists next steps to
  extract search and API, but recent commit history says API, search, vault,
  indexing, runtime, and ingestion were already extracted to sibling repos.
- `README.md` in `projectkoios` lists older component names such as
  `projectkoios-vault`, `projectkoios-ingest`, `projectkoios-schemas`, and
  `projectkoios-cli`; this may not match the current sibling repo layout.
- `projectkoios-notes` describes bootstrap as a temporary external harness, but
  some current operational work is happening in `projectkoios-bootstrap`.
- The next MVP candidate is unclear from current evidence:
  - `projectkoios-agent`
  - `projectkoios-workflow`
  - `projectkoios-obsidian`
  - mothership current-state cleanup before choosing an MVP

## Authority order for this routing task

1. Explicit user instruction
2. Current repository state
3. Passing tests and executable validation
4. Accepted ADRs
5. Repository docs and roadmap
6. `projectkoios-notes` knowledge context
7. Agent inference

## Recommended next route

Ask Archon one bounded architecture question:

> Given the current `projectkoios` repo state, recent extraction commit,
> accepted ADRs, and `projectkoios-notes` knowledge context, what is the current
> role of the mothership repository after extraction, which docs are
> authoritative, which docs should be updated, and what bounded next work slice
> should come before selecting the next MVP?

## Expected Archon output

Archon should write an implementation-ready artifact to `archon/handoffs/` with:

- `architecture-spec`
- `acceptance-criteria`
- `implementation-brief`
- non-goals
- explicit treatment of stale roadmap/ADR/README claims
- explicit statement on whether `chunking` and `repositories` remain in the
  mothership for now
- explicit recommendation on whether to do mothership current-state cleanup
  before starting `projectkoios-agent`, `projectkoios-workflow`, or
  `projectkoios-obsidian`

## Non-goals

- Do not modify `~/repos/projectkoios` until the architecture clarification is
  produced or the user explicitly directs a direct patch.
- Do not modify `~/repos/projectkoios-notes`; treat it as knowledge context.
- Do not commit `.pi/` or other local runtime state from sibling repos.
- Do not turn this into a broad project roadmap rewrite.

## Completion criteria

- Archon artifact exists and is scoped to the bounded question above.
- Codex reviews the Archon artifact against actual repo state.
- The next patch or downstream handoff is clear.
- Any implementation that follows has validation appropriate to the touched
  repository.
