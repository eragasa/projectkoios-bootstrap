Origin: archon
Created: 2026-06-29 23:15
From: Athena
To: Vulcan
Status: complete

# Bootstrap worktree cleanup and Vulcan handoff

## Scope

Clean up and normalize the current `projectkoios-bootstrap` worktree state only.

In scope:
- repo-local tracked/untracked worktree review
- handoff artifact cleanup guidance
- bootstrap validation work triage
- implementation-ready cleanup plan for Vulcan

Out of scope:
- sibling repos under `~/repos/`
- product/domain architecture outside bootstrap/meta-harness concerns
- machine-local runtime config under home directories
- Athena making code changes in this pass

## Current state summary

Observed tracked changes:
- `src/python/projectkoios/bootstrap/cli.py`
- `archon/handoffs/20260629.221927_next-task-bootstrap-stabilization.md`

Observed untracked implementation work:
- `src/python/projectkoios/bootstrap/commands/validate_harnesses.py`
- `src/python/projectkoios/bootstrap/validation/__init__.py`
- `src/python/projectkoios/bootstrap/validation/harnesses.py`
- `tests/test__validate_harnesses.py`
- `archon/handoffs/20260629.230500_harness-validation-framework-spec.md`

Observed local-only/stale working files not suitable for commit as feature output:
- `.README.md.swp`
- `.archon-runs/*.log`
- `.mypy_cache/`
- `.ruff_cache/`
- `.opencode/node_modules/`
- `graphify-out/` contents

Recent relevant artifacts:
- `archon/handoffs/20260629.230500_harness-validation-framework-spec.md`
- `opencode/handoffs/20260629.223200_bootstrap-stabilization-implementation-report.md`
- `goose/handoffs/20260629.221400_repo-state-and-handoff-provenance.md`

## Architecture assessment

### Safe for Vulcan to continue

1. **Bootstrap validation feature branch-in-progress**
   - `src/python/projectkoios/bootstrap/cli.py`
   - `src/python/projectkoios/bootstrap/commands/validate_harnesses.py`
   - `src/python/projectkoios/bootstrap/validation/`
   - `tests/test__validate_harnesses.py`
   - Why safe: these files are directly aligned with Athena’s approved spec in `archon/handoffs/20260629.230500_harness-validation-framework-spec.md`.
   - Constraint: Vulcan must treat the current implementation as partial/inaccurate, not as already accepted.

2. **Status/context update in `archon/handoffs/20260629.221927_next-task-bootstrap-stabilization.md`**
   - Safe to preserve if it accurately reflects the bootstrap stabilization outcome.
   - Vulcan should not spend effort rewriting it unless needed for consistency.

### Not yet acceptable as complete

The current validator implementation is not ready to treat as done. A direct repo-local validation run reports false positives and over-broad checks, including:
- `AGENTS.md` references to `handoffs/` and `pi/handoffs/`
- `agents/global/*.example` references being resolved as if they were repo-root operational docs
- `goose/AGENT.md` reference parsing issues
- `opencode/checklists/multi-repo-execution-readiness.md` references that point outside the validator’s intended repo-local contract surface
- `archon/prompts/harness-routing.md` semantic snippet checks expecting names not required by the approved spec

Therefore the implementation should be continued, but narrowed and corrected against the approved acceptance criteria before merge.

## Stale / rename / consolidate guidance

### Stale or local-only artifacts

These should be treated as disposable local state unless the user explicitly wants them preserved:
- `.README.md.swp`
- `.archon-runs/`
- cache directories (`.mypy_cache/`, `.ruff_cache/`)
- `.opencode/node_modules/`
- `graphify-out/`

### Historical handoff naming drift

The repo still contains older date-only handoff files, including:
- `archon/handoffs/2026-06-29_graphify-out-stale-cleanup-note.md`
- `archon/handoffs/2026-06-29_handoff-naming-convention.md`
- `archon/handoffs/2026-06-29_harness-validation-framework.md`
- `opencode/handoffs/2026-06-29.220345_handoff-naming-and-session-protocol.md`
- `opencode/handoffs/2026-06-29_multi-repo-extraction-report.md`

Guidance:
- Do **not** rename historical artifacts just to satisfy the new convention unless the user asks for history cleanup.
- Do consolidate active guidance by treating the timestamped files as canonical for current work.
- For the validation feature, prefer the timestamped Archon spec:
  - canonical: `archon/handoffs/20260629.230500_harness-validation-framework-spec.md`
  - older precursor to mark as superseded in practice: `archon/handoffs/2026-06-29_harness-validation-framework.md`

### Artifact status cleanup

- `opencode/handoffs/20260629.223200_bootstrap-stabilization-implementation-report.md` still says `Status: active` even though it reads like a completed implementation report.
- Vulcan should either:
  - update status semantics in a follow-up implementation report, or
  - leave the historical file unchanged and issue a new report that clearly supersedes it.

Preferred approach: avoid mutating historical reports unless needed; issue a fresh timestamped report for this cleanup/validation pass.

## Implementation plan for Vulcan

1. **Triage worktree state**
   - Confirm tracked vs untracked files.
   - Separate commit-worthy repo files from disposable local state.

2. **Refine the validation implementation already started**
   - Keep the current command/module/test direction.
   - Reduce scope to the approved manifest and repo-local contract checks.
   - Remove false-positive path extraction behavior.
   - Ensure example-config checks validate coverage/existence without pretending example docs are live repo-root docs.
   - Align semantic assertions strictly to Athena’s approved spec, not ad hoc snippet expectations.

3. **Decide minimal documentation cleanup needed for repo consistency**
   - Only if required by the validator contract.
   - Do not broaden into unrelated documentation rewrites.

4. **Normalize handoff continuity**
   - Use new timestamped filenames and required headers for all fresh artifacts.
   - In the new Vulcan report, explicitly note which earlier artifacts are superseded, still active, or historical-only.

5. **Return a new implementation report**
   - Place it in `opencode/handoffs/`.
   - Include files changed, discarded local-only artifacts, validations run, unresolved deviations, and whether the repo is ready for commit.

## Validation commands Vulcan should run

Run the smallest repo-relevant gates for this repo after cleanup/refinement:

```bash
git status --short
pytest
ruff check .
mypy src/python
python3 -m projectkoios.bootstrap.cli bootstrap validate-harnesses
```

If the editable install/layout requires path-based execution during development, Vulcan may use an equivalent repo-local invocation, but the final report must state the exact command used.

## Acceptance criteria for Vulcan

Vulcan’s cleanup pass is acceptable when all of the following are true:

1. Only repo-scope, commit-worthy files remain in the intended change set.
2. Disposable local-only artifacts are either removed from the worktree or explicitly excluded from the implementation scope/report.
3. The active validation work remains aligned with `archon/handoffs/20260629.230500_harness-validation-framework-spec.md`.
4. The validator no longer fails on false positives caused by over-broad reference parsing or incorrect manifest assumptions.
5. `pytest`, `ruff check .`, and `mypy src/python` pass, or any failure is documented as a spec deviation/blocker.
6. The validator command runs deterministically against this repo and reports findings with the approved severity model.
7. Vulcan writes a fresh timestamped implementation report in `opencode/handoffs/` that identifies:
   - safe continued local changes
   - stale/historical artifacts
   - any superseded handoffs
   - exact validation commands and results

## Handoff note

Athena’s boundary ends here: plan/spec only.
Vulcan owns implementation, file cleanup decisions, test execution, and any repo modifications needed to realize this cleanup plan.
