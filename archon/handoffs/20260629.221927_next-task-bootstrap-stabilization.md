Origin: pi
Created: 2026-06-29 22:19
From: pi
To: Athena
Status: draft

# Task: bootstrap stabilization next step

## Context

We are working in a single project only: `projectkoios-bootstrap`.
The user is manually switching between harnesses for now.

Current operational constraints observed:
- Archon is down / was not available for the original review run.
- `tmux` is not installed or not on PATH in the current environment, so
  `./scripts/koios harnesses ...` cannot start or inspect the session here.
- I already made two lightweight bootstrap fixes in-repo:
  - `src/python/projectkoios/bootstrap/commands/harnesses.py`
    - fixed `_in_tmux()` to use `os.environ` instead of invalid `sys.environ`
  - `scripts/README.md`
    - fixed `docs/architecture.00.md` → `doc/architecture.00.md`

## Request

Please define the **next concrete task** for Athena with the smallest useful scope.
My current recommendation is:
1. stabilize the bootstrap/runtime path for this repo
2. then stand up Goose next
3. then resume Archon planning/ADR work

## Output needed

Return a short, implementation-ready architecture decision or task plan that:
- stays scoped to `projectkoios-bootstrap`
- does not assume tmux can be launched from inside a harness
- reflects the one-project constraint
- identifies the next best operational step
